#include <stdio.h>
int main() {
    int a[10],n,i,sum=0;
    printf("Enter size:");
    scanf("%d",&n);
    printf("Enter the elements:");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
      for(i=0;i<n;i++)
      {
          sum=sum+a[i];
      }
      printf("The sum of the elements of the array is %d",sum);
}
